import 'package:flutter/material.dart';

class RegionBeijingPage extends StatelessWidget {
  const RegionBeijingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('北京'));
  }
}
