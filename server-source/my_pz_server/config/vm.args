-name 'my_pz_server@127.0.0.1'

-setcookie my_pz_server_cookie

+K true



## 启动调度器线程数（等于CPU核心数）
+S 16:16

## 异步线程池（处理文件/端口/IO）
+A 32

## 启用 JIT 编译器（如果支持）
+J

## 增加进程限制（默认 262144，够用了可以不动）
+P 1048576

## 压缩栈（减小内存）
+stbt db

## 内存分配器调优（减少碎片）
+MBas ageffcbf

## 增强日志（启用 TTY + crash dump）
-env ERL_CRASH_DUMP "./log/erl_crash.dump"
-env ERL_CRASH_DUMP_SECONDS 10

## 设置最大 dirty scheduler 数量
+SDio 16
+SDcpu 16
