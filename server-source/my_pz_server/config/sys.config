[
  {my_pz_server, 
      [ 
        {kernel, [
            {inet_dist_listen_min, 9100},
            {inet_dist_listen_max, 9105}
          ]},
          {db,
            [
              {name, mysql},
              {poolConf, [{size, 10}, {max_overflow, 20}]},
              {sqlConf,
              [
                  {host, "127.0.0.1"},
                  {port, 3306},
                  {user, "root"},
                  {password, "123"},
                  {database, "xiyi"},
                  {keep_alive, true},
                  {prepare, [{foo, "select SYSDATE()"}]}
              ]
              }
            ]
          }

        
      ]
  }
].




